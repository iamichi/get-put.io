# get-put.io backend

FastAPI backend for the `get-put.io` control plane.

This package exposes:
- API endpoints for health, metadata, and sync previews
- service boundaries for Put.io, `rclone`, and Jellyfin
- the server entrypoint used by native development and container builds

